# Final Cleanup Report

Date: 2026-05-18

Scope: audit only. No files were deleted or moved while creating this report.
SQL Server is the company-ready target path; PostgreSQL remains as fallback
until final approval.

## Current Git State

- Current branch: `phase5-wip-backup`
- Upstream shown by git: `origin/final`
- Current HEAD: `ff14272 merge: integrate portable SQL Server cache and logging`
- Working tree before documentation edits: clean
- `backend/.env`: ignored local file, not staged, not modified by this audit

Recent commits:

```text
ff14272 merge: integrate portable SQL Server cache and logging
f37b6ce feat(sqlserver): add portable plan cache and query logging
3bf021f feat(sqlserver): complete live SQL Server smoke tooling
f01176b docs(sqlserver): record live smoke test and working odbc URL
6ec309b fix(sqlserver): add connection prober and CSV staging loader
1db8517 fix(sqlserver): make fact_sales_line import columns nullable
984e504 fix(sqlserver): bracket reserved metadata columns
2580c96 feat(sqlserver): add schema scripts and portable catalog loader
```

## 1. Safe To Delete

These are generated or local-only files. Delete only after user confirmation.

| Path | Reason |
|---|---|
| `**/__pycache__/` outside `.venv/` | Python bytecode cache; regenerated automatically. |
| `.pytest_cache/` | pytest runtime cache; regenerated automatically. Current scan hit an access-denied warning, so use normal cleanup tools carefully. |
| `.mypy_cache/` | mypy runtime cache; regenerated automatically. |
| `scripts/__pycache__/` | Python bytecode cache from smoke/probe scripts. |
| `backend/**/__pycache__/` | Python bytecode cache from backend tests/runtime. |
| `backend/db/sqlserver/__pycache__/` | Generated by running `_gen_seed.py`; not source. |
| `docs/.bi_meta_data.txt` | Ignored transient audit dump from earlier semantic catalog work. |
| `docs/.bi_meta_dump.txt` | Ignored transient audit dump from earlier semantic catalog work. |
| `docs/.bi_meta_export.json` | Ignored transient audit export from earlier semantic catalog work. |
| `docs/.bi_meta_strings.txt` | Ignored transient audit dump from earlier semantic catalog work. |
| `docs/.sqlserver_audit.txt` | Ignored transient SQL Server audit output. |
| `aac_repo_backup.bundle` | Ignored local git bundle backup; move off-repo or delete after confirming another backup exists. |
| `frontend/node_modules/` | npm dependency tree; regenerated by `npm install`. |
| `model_cache/` | Local model cache; can be regenerated or re-downloaded, but delete only when disk cleanup is desired. |

Specific requested item:

- Root-level `load_sqlserver_staging.py`: not present. The canonical loader is
  `scripts/load_sqlserver_staging.py` and should be kept.

## 2. Move To Archive

These have human-authored content or historical value. Prefer archiving or
keeping until the user approves a cleanup pass.

| Path | Recommendation |
|---|---|
| `.claude/` | Local agent/worktree state. Move out of the repo or delete only after confirming no active worktree is needed. |
| `tools/test.ps1` | Likely old diagnostic wrapper. Archive if superseded by pytest and SQL Server smoke scripts. |
| `tools/test_llm_overrides_rulebased_cache.py` | Likely specialized one-off diagnostic. Archive if no current workflow references it. |
| `tools/new-env-pilot.ps1` | Environment helper of uncertain current value. Archive if obsolete. |
| `tools/restore_phase4.ps1` | Historical Phase 4 bootstrap helper. Archive only after SQL Server runbook is accepted. |
| `tools/archive/bootstrap_db_docker.ps1.bak.20260302_163638` | Already an archive backup; can be moved to external archive or deleted after approval. |
| `tools/archive/backups/` | Ignored backup tree with many `.bak` snapshots. Move out of repo or delete after approval. |
| `01_prepare_bi_data_notebook.ipynb` | Data-preparation notebook. Keep if onboarding still uses it; otherwise move to `docs/archive/` or `notebooks/`. |
| `request_test.py` | Old root-level API probe. Archive or move under `scripts/` if still useful; SQL Server smoke now lives in `scripts/smoke_sqlserver_ask.py`. |
| `docs/CLEANUP_REPORT.md` | Earlier Phase A cleanup report. Keep as history or move to `docs/archive/` after this final report is accepted. |
| `docs/BI_META_AUDIT.md` | Historical semantic catalog audit. Keep if still useful for traceability; otherwise archive. |
| `docs/RUN.md` | Very small legacy run note. Archive if README becomes the canonical runbook. |

## 3. Keep

These are active or explicitly protected for the SQL Server-first baseline.

- `backend/db/sqlserver/`
- `scripts/test_sqlserver_connection.py`
- `scripts/load_sqlserver_staging.py`
- `scripts/smoke_sqlserver_ask.py`
- `docs/SQLSERVER_MIGRATION_PLAN.md`
- `docs/DATABASE_MIGRATION.md`
- `docs/PHASE_C_PLAN.md`
- `backend/app/db/sqlserver_connect.py`
- `backend/app/db/adapter.py`
- `backend/app/services/catalog_loader.py`
- `backend/app/services/ask/cache_log.py`
- `backend/app/services/ask/runner.py`
- `backend/tests/test_sqlserver_connect.py`
- `backend/tests/test_ask_cache_log.py`
- `backend/tests/test_sqlserver_live_ask.py`
- PostgreSQL fallback code and `db/` SQL files
- `compose.db.yml` for fallback PostgreSQL tests
- `data/clean/bi_ready_clean.csv` and raw/quality data
- `frontend/` source tree
- `backend/.env.example` and `frontend/.env.example`

## 4. Unsure / Ask User

Ask before deleting, moving, or rewriting these.

- Local branches:
  - `main`
  - `phase5-wip-backup`
  - `sqlserver-phase-a-dialect-foundation`
  - `sqlserver-phase-b-db-access-abstraction`
  - `sqlserver-phase-c-python-compiler`
  - `sqlserver-phase-c2-ask-python-compiler`
  - `sqlserver-phase-c3-sqlserver-schema-catalog`
  - `sqlserver-phase-c4-live-sqlserver-smoke`
  - `sqlserver-phase-c5-portable-cache-log`
  - `claude/confident-carson-015b20`
- Remote branches:
  - `origin/final`
  - `origin/sqlserver-phase-a-dialect-foundation`
  - `origin/sqlserver-phase-c4-live-sqlserver-smoke`
  - `origin/sqlserver-phase-c5-portable-cache-log`
- `db/arabic_analytics_backup.sql`: tracked SQL backup; keep until the team confirms PostgreSQL fallback no longer needs it.
- `db/db_data.sql`, `db/db_schema.sql`, `db/db_dump.sql`: tracked PostgreSQL fallback assets; keep for now.
- `.gitignore`: currently ignores the right cleanup candidates, but its displayed content includes unusual null-byte-looking characters around `.claude/`; inspect before changing.

No branches should be deleted automatically. After merge approval, the likely
branch cleanup candidates are the completed `sqlserver-phase-*` branches and
`claude/confident-carson-015b20`, but only after confirming no open work depends
on them.

## README Changes Proposed

The root README should become the company-facing SQL Server-first runbook:

- State SQL Server as the operational target and PostgreSQL as fallback.
- Document SQL Server prerequisites.
- Show how to create `ArabicAnalytics` with scripts `001` through `005`.
- Explain Windows Authentication and SQL Authentication environment variables.
- Require SQLAlchemy `odbc_connect=` style for named instances.
- Show CSV load command with `scripts/load_sqlserver_staging.py`.
- Show backend and frontend run commands.
- Show SQL Server connection and `/api/ask` smoke tests.
- Document expected C5 smoke results: health 200, ask 200, query log id, plan cache reuse, `skipped_for_sqlserver=None`.
- Include troubleshooting for ODBC driver, Mixed Mode, Arabic encoding, named instance URLs, and PostgreSQL fallback tests.

## Tests To Run

Focused SQL Server/C5 tests:

```powershell
.\.venv\Scripts\python.exe -m pytest backend/tests/test_sqlserver_connect.py backend/tests/test_ask_cache_log.py backend/tests/test_ask_compiler_backend.py backend/tests/test_sqlserver_live_ask.py -q
```

SQL Server connection probe:

```powershell
python scripts/test_sqlserver_connection.py
```

SQL Server live smoke:

```powershell
python scripts/smoke_sqlserver_ask.py
```

Full test suite, when PostgreSQL fallback Docker is available:

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

If full pytest fails only with PostgreSQL `127.0.0.1:5432` connection errors,
classify that as a missing fallback environment rather than a SQL Server
regression.

## Suggested Commit Message

```text
docs(sqlserver): add production readiness cleanup report
```
