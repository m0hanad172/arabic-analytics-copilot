# Final Cleanup Report

Date: 2026-05-18

Scope: audit only for production readiness. SQL Server is the company-ready
runtime. PostgreSQL and Docker fallback assets are intentionally retained until
a separate final removal phase is approved.

## Current Git State

- Current branch: `phase5-wip-backup`
- Remote target: `origin/final`
- Current baseline includes SQL Server portable cache/logging and planner fixes
- `backend/.env`: ignored local file, not modified or staged by this audit

## Safe Deleted Already

The following generated/local artifacts were removed in the prior safe cleanup
pass:

- non-`.venv`, non-`frontend/node_modules` `__pycache__/` directories
- `.pytest_cache/`
- `.mypy_cache/`
- `docs/.bi_meta_data.txt`
- `docs/.bi_meta_dump.txt`
- `docs/.bi_meta_export.json`
- `docs/.bi_meta_strings.txt`
- `docs/.sqlserver_audit.txt`
- untracked local `*.bundle` backup files

No tracked source files were removed.

## Safe To Delete Now

Delete only after confirmation and only if present:

| Path | Reason |
|---|---|
| `**/__pycache__/` outside `.venv/` and `backend/db/sqlserver/` | Generated bytecode. |
| `.pytest_cache/` | Generated pytest cache. |
| `.mypy_cache/` | Generated mypy cache. |
| `docs/.bi_meta_*.txt` | Generated catalog audit dumps. |
| `docs/.bi_meta_*.json` | Generated catalog audit exports. |
| `docs/.sqlserver_audit.txt` | Generated SQL Server audit output. |
| root `*.bundle` files | Local git backup bundles if untracked. |

Do not delete `frontend/node_modules/` in this pass unless explicitly
confirmed as local-only and untracked.

## Keep

Active SQL Server production-readiness files:

- `backend/db/sqlserver/`
- `backend/app/db/sqlserver_connect.py`
- `backend/app/db/adapter.py`
- `backend/app/db/session.py`
- `backend/app/services/catalog_loader.py`
- `backend/app/services/ask/cache_log.py`
- `backend/app/services/ask/runner.py`
- `backend/app/services/ask/plan_normalize.py`
- `backend/app/services/semantic/compiler.py`
- `scripts/test_sqlserver_connection.py`
- `scripts/load_sqlserver_staging.py`
- `scripts/smoke_sqlserver_ask.py`
- `scripts/acceptance_sqlserver_questions.py`
- `docs/SQLSERVER_MIGRATION_PLAN.md`
- `docs/DATABASE_MIGRATION.md`
- `docs/PHASE_C_PLAN.md`
- `docs/ACCEPTANCE_TESTS.md`
- `backend/.env.example`
- `frontend/.env.example`
- `data/clean/bi_ready_clean.csv`
- `frontend/` source tree

Active tests:

- `backend/tests/test_sqlserver_connect.py`
- `backend/tests/test_ask_cache_log.py`
- `backend/tests/test_ask_compiler_backend.py`
- `backend/tests/test_sqlserver_live_ask.py`
- `backend/tests/test_python_compiler.py`
- PostgreSQL fallback tests until fallback removal is approved

## Archive Later

Archive only after user confirmation:

| Path | Recommendation |
|---|---|
| `.claude/` | Local agent/worktree state. It is ignored/local, but remove only after confirming no active worktree is needed. |
| `tools/test.ps1` | Old diagnostic wrapper; archive if superseded by pytest and acceptance scripts. |
| `tools/test_llm_overrides_rulebased_cache.py` | Specialized diagnostic; archive if no current workflow uses it. |
| `tools/new-env-pilot.ps1` | Older environment helper; archive if obsolete. |
| `tools/restore_phase4.ps1` | Historical Phase 4 helper; archive after SQL Server runbook acceptance. |
| `tools/archive/backups/` | Ignored historical backups; move off-repo or delete after approval. |
| `01_prepare_bi_data_notebook.ipynb` | Keep if onboarding uses it; otherwise move to `notebooks/` or archive. |
| `request_test.py` | Old root API probe; archive or move under `scripts/` if still useful. |
| `docs/CLEANUP_REPORT.md` | Earlier cleanup report; keep for history or archive after final docs are accepted. |
| `docs/BI_META_AUDIT.md` | Historical semantic audit; keep for traceability or archive. |
| `docs/RUN.md` | Legacy short run note; archive if README and acceptance docs replace it. |

## PostgreSQL Removal Candidates

Do not remove in this stage. Candidate areas for a future approved
SQL-Server-only phase:

- `compose.db.yml`
- `db/db_dump.sql`
- `db/db_schema.sql`
- `db/db_data.sql`
- `db/arabic_analytics_backup.sql`
- PostgreSQL-specific branches in `backend/app/db/adapter.py`
- PostgreSQL dialect support if no fallback deployments remain
- tests that exist only for PostgreSQL fallback behavior

Removal prerequisites:

- SQL Server acceptance suite passes in the target company environment
- final approval confirms PostgreSQL fallback is no longer required
- replacement migration/backup/runbook exists for SQL Server-only operation

## Docker Removal Candidates

Do not remove in this stage. Candidate files for later review:

- `compose.db.yml`
- Docker-specific README/runbook sections
- PostgreSQL Docker restore instructions

Keep Docker assets while PostgreSQL fallback tests or recovery workflows still
depend on them.

## Branch Cleanup Candidates

Do not delete branches automatically.

Potential later cleanup after merge approval:

- completed `sqlserver-phase-*` local branches
- matching completed remote feature branches
- `claude/confident-carson-015b20` after `.claude/` worktree confirmation

Keep:

- `phase5-wip-backup` until final push/merge is accepted
- `origin/final`
- any branch still used for audit or rollback

## Final Checks To Run

```powershell
.\.venv\Scripts\python.exe -m pytest backend/tests/test_sqlserver_connect.py backend/tests/test_ask_cache_log.py backend/tests/test_ask_compiler_backend.py backend/tests/test_sqlserver_live_ask.py backend/tests/test_python_compiler.py -q

python scripts/test_sqlserver_connection.py
python scripts/smoke_sqlserver_ask.py
python scripts/acceptance_sqlserver_questions.py
```

If full pytest fails only because PostgreSQL Docker is unavailable or the local
environment is SQL Server-first, classify that as an environment/fallback issue,
not a SQL Server regression.
