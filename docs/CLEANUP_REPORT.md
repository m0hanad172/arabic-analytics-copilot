# Cleanup Report (Phase A)

This report only **classifies** files. **Nothing is deleted or moved in
Phase A.** Use it as the basis for a later cleanup commit.

## Branch audit (captured during Phase A)

- **Current branch:** `sqlserver-phase-a-dialect-foundation` (created
  from `origin/final` for this phase).
- **Local branches:**
  - `phase5-wip-backup` — tracks `origin/final`, currently at the same
    commit as the remote tip. Keep as a safety backup.
  - `sqlserver-phase-a-dialect-foundation` — this phase's working branch.
  - `main` — present locally but not the project baseline; do not commit
    to it without explicit instruction.
  - `claude/confident-carson-015b20` — Claude Code worktree branch under
    `.claude/worktrees/`. Belongs to the agent worktree, not to the
    project; safe to delete after the worktree is removed.
- **Remote branches:**
  - `origin/HEAD -> origin/final` — origin's default branch.
  - `origin/final` — **the project baseline.**
- **Recommended baseline branch:** `origin/final` (origin/HEAD points to
  it; `phase5-wip-backup` confirms the team treats it as the trunk).
- **Branches that may be deleted later (after explicit confirmation):**
  - `claude/confident-carson-015b20` (after the agent worktree is
    closed).
- **Branches that must be kept:**
  - `origin/final`, `phase5-wip-backup`, `main`, and the active
    `sqlserver-phase-a-*` feature branches.

> No branches are deleted in Phase A.

## 1. Safe to delete (generated / cache, no human-authored content)

| Path | Reason |
|---|---|
| `**/__pycache__/` | Python bytecode cache, auto-regenerated. |
| `**/.pytest_cache/` | pytest cache, auto-regenerated. |
| `**/*.pyc` | Compiled bytecode. |
| `frontend/node_modules/` | npm dependency tree, regenerated by `npm install`. |
| `model_cache/` | Local model artefacts; verify with the team before deleting. |
| `dist/`, `build/` (if they appear) | Build artefacts. |
| `*.log` (project root) | Run logs. |

These should be covered by `.gitignore`. Verify before any cleanup commit.

## 2. Move to `archive/` later (human-authored but possibly unused)

| Path | Why it's a candidate | Risk |
|---|---|---|
| `backend/app/utils/sql_safety.py` | Older, weaker guardrails. Superseded by `core/sql_guardrails.py`. Confirm no live importers (`grep -R sql_safety backend/`) before archiving. | **Do NOT archive yet** — verify all callers are migrated to `guard_sql_or_raise` first. |
| `01_prepare_bi_data_notebook.ipynb` (repo root) | Data-prep notebook, not part of the runtime. | Low — keep under `notebooks/` or `archive/`, but confirm with the author it isn't part of an onboarding flow. |
| `aac_repo_backup.bundle` (repo root) | Local git bundle backup. Already gitignored per recent commits. | Low — typically should not live in the worktree at all; move to a backup location off-repo. |
| `request_test.py` (repo root) | Looks like an ad-hoc HTTP probe script. | Medium — confirm with author; if kept, move to `scripts/`. |

## 3. Keep (in active use)

- `backend/app/main.py` and all wired routers under
  `backend/app/api/routes/`: `ask.py`, `query.py`, `schema.py`,
  `health.py`, `logs.py`, `eval.py`, `transcribe.py`.
- `backend/app/core/{config,sql_guardrails,llm_gate}.py` — runtime
  config + active guardrails (now dialect-aware).
- `backend/app/db/{session,introspect}.py` and the new
  `backend/app/db/dialects/` package.
- `backend/app/services/{catalog_cache,query_service}.py`.
- `backend/app/sqlgen/{translate,glossary}.py`.
- `backend/app/utils/sql_safety.py` — keep until callers migrate to the
  new guardrails (Phase B).
- `backend/app/models/schemas.py`.
- `backend/.env.example`, `backend/RUN.md`, `backend/requirements.txt`.
- `backend/tests/conftest.py`, `backend/tests/golden_questions.jsonl`,
  `backend/tests/quality_cases.jsonl`, every existing `test_*.py`,
  and the new `test_dialects.py` / `test_translate_dialects.py` /
  `test_guardrails_dialects.py`.
- `frontend/` (React/Vite/TS app) and its `package.json`,
  `eslint.config.js`, `tsconfig*.json`, `index.html`, `public/`, `src/`.
- `db/`, `scripts/`, `tools/` — operational SQL and tooling.
- `data/` — source data for development.
- `docs/` (existing `RUN.md` + this report and the migration doc added
  in Phase A).
- `compose.db.yml`, `pytest.ini`, `package.json` (root), `package-lock.json`.

## 4. Recommended order for the eventual cleanup commit

1. Delete generated caches (`__pycache__`, `.pytest_cache`).
2. Confirm `aac_repo_backup.bundle` is not tracked (git history shows it
   was added to `.gitignore`); if a copy still sits in the working tree,
   move it off-repo.
3. Audit callers of `backend/app/utils/sql_safety.py`. After Phase B
   migrates them to `core/sql_guardrails.guard_sql_or_raise`, archive
   `utils/sql_safety.py`.
4. Move `01_prepare_bi_data_notebook.ipynb` to `notebooks/` and
   `request_test.py` to `scripts/` (if kept).
5. Tighten `.gitignore` if any of the above were tracked.

## 5. Files NOT to touch in any cleanup pass

- Anything under `data/`.
- Any SQL referenced by the PostgreSQL `bi_meta.*` setup (under `db/`
  or `tools/db/`).
- The `frontend/` source tree (UI work is out of scope for Phase A).
