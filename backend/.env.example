# Backend runtime
APP_ENV=dev
APP_HOST=127.0.0.1
APP_PORT=8000

# Postgres (your Docker mapping is localhost:5433 -> container:5432)
DATABASE_URL=postgresql+asyncpg://copilot_loader:ChangeMe_Strong!@localhost:5433/arabic_analytics

# Allowlist (semantic layer)
ALLOWED_SCHEMA=bi

# Safety
DEFAULT_MAX_ROWS=200
STATEMENT_TIMEOUT_MS=8000
